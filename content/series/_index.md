---
title: "Серии"
# The series taxonomy carries metadata only. This node (the /series/ list) never
# renders; the config cascade suppresses the term pages under /series/**.
build:
  render: never
  list: never
---
