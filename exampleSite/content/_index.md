---
title: "taiga"
# The share card's text, and only that. It is deliberately NOT the hero line:
# the hero is lowercase on purpose and is already burned into the OG poster, so
# reusing it would make a chat preview read the same phrase twice — once in the
# image, once under it. A share card has no styling and no context; it gets a
# whole sentence.
description: "A Hugo theme for learning platforms — rubrics, course series and interactive widgets living inside the text."
# Home hero copy lives in [params] (heroLine / heroSat) — one edit for the site
# owner. The full home layout (hero, rubric cards, featured, feed, wip strip) is
# assembled by the theme's home.html.
---
