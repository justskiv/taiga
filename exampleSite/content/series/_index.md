---
title: "Серии"
# The series taxonomy carries metadata only. This node (the /series/ list) never
# renders; the config cascade suppresses the term pages under /series/**. Series
# metadata (title, description, weight) lives in the term _index files.
build:
  render: never
  list: never
---
